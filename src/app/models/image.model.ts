export class Image {
    height: number;
    url: string;
    width: number;

    constructor(height: number, url: string, width: number) {
        this.height = height;
        this.url = url;
        this.width = width;
    }
}